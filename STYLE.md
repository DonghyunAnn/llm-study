# 집필 규칙 {.unnumbered}

이 책의 모든 노트가 따르는 규칙이다. 웹과 PDF 어느 쪽으로 읽어도 같은 품질이 나오도록 한다.

## 구조

- 제목(H1) 하나, 그 아래 H2로 절, H3까지만 쓴다. 절 번호는 빌드 시 자동으로 붙으므로 제목에 숫자를 넣지 않는다.
- 노트 머리말에는 원본(강의·논문 링크, 저자, 날짜, 길이), 정리 방식과 한계, 검수 여부를 적는다.
- 강의 노트는 강의 순서를 따르되, 맨 앞에 "구조와 minimind 대응" 표를 두어 전체를 한눈에 보게 한다.
- 노트 끝에는 "직접 볼 구간"(화면이 중요한 데모), "이해 확인 질문", "실습으로 이어지는 지점"을 둔다.

## 문장

- 본문은 한국어, 기술 용어는 영어 그대로 쓴다(reward model, attention, RLHF). 한 개념에 한 표현만 쓴다.
- 강의자·저자의 주장과 정리자의 해석을 구분한다. 강의자가 추측이라고 밝힌 것은 "~라고 본다", "~일 것으로 추정한다"로 옮긴다.
- 자막·논문 밖에서 가져온 정보는 `(설명란)`, `(논문)`, `(보충)`으로 표시한다.
- 수치는 원본의 단위와 조건을 함께 적는다.

## 상자(콜아웃)

- `minimind 대응` — 같은 개념이 minimind 코드 어디에 있는지. 파일과 함수 이름을 반드시 쓴다.
- `주의` — 오해하기 쉬운 지점, 강의와 현재 실무의 차이.
- `해보기` — 독자가 직접 실행할 것.

## 코드

- 강의 repo 코드는 링크가 아니라 본문에 싣는다. 파일 → 함수 → 줄 순서로 분해하고, 각 줄에 `# 각주:`를 단다.
- 블록마다 (1) 무엇을 하는지, (2) 왜 그렇게 짰는지, (3) minimind 대응을 적는다.
- 라이선스와 커밋 해시를 머리말에 적고, 실행 결과를 기록한다.

## 그림

- 도식은 `diagram-design` 스킬로 만들고 원본 HTML은 노트 폴더의 `diagrams/`에 둔다. 본문에는 PNG를 넣고 캡션에 출처를 적는다.
- 논문 그림은 "Figure N, 저자 연도"로 출처를 적고, 다시 그린 것은 "adapted from"으로 구분한다.

## 검수

- 초안 → Claude 서브에이전트와 Codex의 독립 리뷰(같은 브리프) → 근거로 판정해 반영 → 최종 커밋. 절차는 `CLAUDE.md`, 브리프는 `tools/review_brief_template.md`.
